name: sandclock
description: "Песочные часы"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: 3.11.4

dependencies:
  audioplayers: 6.6.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: 6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/timer_done.wav
